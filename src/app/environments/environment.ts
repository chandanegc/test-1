export const environment = {
  production: false,
  apiUrl: 'https://api.fooddelivery.example.com/v1',
  apiMockDelay: 500, // Simulate network delay for development
  features: {
    enableMockApi: true,
    enableAnalytics: false
  }
};
